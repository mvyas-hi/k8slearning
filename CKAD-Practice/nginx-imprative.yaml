apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nginx
  name: nginx
spec:
  containers:
  - image: kodekloud/webapp-color
    name: nginx
    envFrom:
      - configMapRef:
          name: webapp-config-map
